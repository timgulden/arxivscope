# Local Development Environment Configuration
# Copy this file to .env.local and modify as needed
# This file is NOT committed to the repo - for local use only
#
# Current Environment (October 2025):
# - Local laptop environment (macOS)
# - API on port 5001, React Frontend on port 3000
# - PostgreSQL on port 5432 (internal drive)
# - All data stored on internal drive

# API Configuration
NEW_API_BASE_URL=http://localhost:5001
LEGACY_API_BASE_URL=http://localhost:5001

# Frontend Configuration (React)
NEW_UI_PORT=3000
NEW_UI_PUBLIC_URL=http://localhost:3000

# Legacy Dash Frontend (for reference only, frozen)
DOCSCOPE_PORT=8050

# Database Configuration (Local)
# PostgreSQL runs on port 5432 on internal drive
# Trust authentication (no password needed for local setup)
DB_HOST=localhost
DOC_TROVE_PORT=5432
DB_NAME=doctrove
DB_USER=doctrove_admin
DB_PASSWORD=  # Empty for trust authentication

# UMAP Model Path (Internal Drive)
UMAP_MODEL_PATH=/Users/tgulden/Documents/DocTrove/arxivscope/models/umap_model.pkl

# Other local-specific settings
TARGET_RECORDS_PER_VIEW=5000
DEBOUNCE_DELAY_SECONDS=0.05

# OpenAI Configuration (Personal API Key)
# Note: The corporate RAND API key has been deprecated and should not be used.
# Get your personal API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_personal_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI Model Configuration
# Embedding model for generating vector embeddings
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small  # Used with Azure OpenAI if switching back

# Chat model for LLM features (SQL generation, cluster summaries)
OPENAI_CHAT_MODEL=gpt-4o
OPENAI_CHAT_DEPLOYMENT=gpt-4o  # Used with Azure OpenAI if switching back

# Feature Flags - Enable/disable OpenAI features
USE_OPENAI_EMBEDDINGS=true  # Set to false to disable embedding generation
USE_OPENAI_LLM=true  # Set to false to disable LLM features (SQL generation, summaries)
